name: Self-Hosted Dispatch Control

on:
  workflow_dispatch:

jobs:
  control:
    runs-on: [self-hosted, macOS]
    steps:
      - name: Control check
        run: echo "Self-hosted runner dispatch is working."
